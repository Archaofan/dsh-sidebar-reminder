# DSH 网页版「会话挂起 + 左侧栏高亮提醒」插件调研报告

> 调研对象：DeepSeek Harness（dsh）Web UI 插件生态
> 本地核实基线：`C:\Users\Administrator\AppData\Roaming\DSH\data\versions\0.1.6-alpha.2`（@deepseek-ai/dsh 0.1.6-alpha.2）
> 日期：2026-08（基于当前会话时间）

---

## 0. TL;DR（直接回答）

**没有找到任何一个插件完整实现「对话里自然语言描述 → 左侧会话栏该行高亮 → 悬停显示提醒事项」这三合一。** 但三块能力都有现成实现，且都有官方允许的扩展路径：

| 需求拆解 | 官方内置 | 社区最接近的实现 |
|---|---|---|
| 自然语言描述待办/提醒 | `@deepseek-ai/dsh-schedule`（模型工具 schedule_create/list/delete，需 overlay 启用） | [YOLO](https://github.com/hanshanyike/dsh-yolo)（`dsh-plugin-yolo`，LLM 从对话自动抽取事项+到期提醒+看板） |
| 左侧栏会话行按状态高亮 | 无（官方行只读硬编码的 `schedule` 投影画闹钟） | [dsh-session-colorful-unread-pin-jobs](https://github.com/enterhalf/dsh-session-colorful-unread-pin-jobs)（行标题按状态染色）；[dsh-workspace-kit](https://github.com/ice5kysl/dsh-workspace-kit)（低优先级影子替换整个侧边栏浏览器，行完全自定义） |
| 悬停提示 / 角标 / 未读提醒 | 无 | [dsh-activity-bell](https://github.com/minivv/dsh-activity-bell)（铃铛角标+活动列表，DOM 挂载）；[dsh-web-attention-badge](https://github.com/Luaphes/dsh-web-attention-badge)（纯客户端，边框 pill + 标签页计数 + favicon 变色） |

**结论：自己开发这个插件完全可行，且是社区空白点。** 工作量主要集中在「客户端侧边栏行级 UI」；自然语言捕获和持久化都有成熟范式可抄。建议路线见第 5 节。

---

## 1. 需求拆解（用户原话）

> 项目联调 / 现实硬件重启时需要暂时挂起一个会话；会话多了会忘记还有事情没做完。希望：在对话里用自然语言描述 → 退出到外部对话框后，左侧会话栏对应会话**高亮** → **鼠标悬停提示提醒事项**。

技术动作分解：

1. **捕获**：在会话内用自然语言（不是固定关键词）登记一条「挂起备注」——需要模型理解语义，即注册一个模型工具（tool）让 agent 调用，或挂钩子监听消息。
2. **存储**：按 sessionId 持久化备注（跨刷新、跨重启），并对浏览器可见。
3. **呈现**：
   - a. 左侧会话列表里该会话行视觉高亮（背景/圆角/左侧色条等）；
   - b. 悬停该行时弹出提醒事项 tooltip；
   - c. （可选增强）折叠侧边栏时的角标计数、专门的「挂起事项」面板。
4. **解除**：回到会话后可以查看/完成/删除备注。

---

## 2. 官方内置能力核实（本地 0.1.6-alpha.2 安装目录）

### 2.1 `@deepseek-ai/dsh-schedule` —— 最接近的官方能力

- 形态：host 插件，注册 `schedule_create` / `schedule_list` / `schedule_delete` 三个**模型工具**；用户在对话里说「10 分钟后提醒我 review PR」，模型调用工具创建**会话内**耐用提醒（[官方指南](https://deepseek-harness.github.io/deepseek-harness/guide/schedule)）。
- UI：启用 overlay 后，有活动提醒的会话在**对话 header** 显示只读目录；**侧边栏 grouped/flat/search 行在标题后显示不可交互的闹钟**。
- 与你的需求的差距：
  - 它是**时间触发型**（到点后在同一个会话里排一个 follow-up 轮次），不是「挂起备注 + 随时查看」；
  - 提醒内容不会以悬停 tooltip 形式出现在侧边栏行上；
  - **行的闹钟是硬编码的**：本地 bundle 里判定为 `session.projectionValues?.schedule?.length > 0`（见 `node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/client.js` 第 464-465 行），即只认官方 `schedule` 这一个投影 key。**第三方插件自己注册的投影 key 不会在行上渲染任何东西**（第 4.4 节详述）。
- 启用方式：官方文档是从源码检出运行时执行 `dsh web --patch apps/cli/config/examples/schedule/cordis.yml`；npm 安装的 dsh 包内不带该示例文件，需要等价的 patch 层（在 profile 的 `cordis.patch.yml` 里 insert `@deepseek-ai/dsh-schedule` 行，具体行内容以官方示例为准）。

### 2.2 官方 slot 体系里侧边栏可用的扩展位（本地类型文件核实）

`dsh-client-ui-sidebar` 声明的 slot（`lib/types/client/contract/slots.d.ts`）：

| Slot | 类型 | 对本需求的用途 |
|---|---|---|
| `sidebar.toggle.badge` | single / root | **折叠侧边栏 Expand 按钮内的角标**——放「N 个挂起会话」计数 |
| `sidebar.panellist` | list / root | 注册一个全局面板行（类似官方 Plugins 页）——做「挂起事项」总览面板 |
| `sidebar.workspaces` | single / root | 整个会话浏览区域。官方注释明确「declaring is claiming」，但社区已验证可以**低优先级影子替换**（见 3.2） |
| `sidebar.footer.action` | list / root | 侧边栏底部按钮——YOLO 的看板入口、dsh-activity-bell 的铃铛都在这个位置 |
| `sidebar.settings` | single / root | 设置 seat（不相关） |

`dsh-client-ui-conversation` 声明的会话级 slot（`lib/types/client/contract/slots.d.ts`）：

| Slot | 类型 | 用途 |
|---|---|---|
| `conversation.session.header.actions` / `.corner` / `.utilities` | list/single / session | 打开某个会话时，在 header 放「📌 挂起并添加备注 / 查看备注」按钮 |
| `conversation.input.right` | list / session | composer 工具行按钮（dsh-session-guard 的「暂停会话」按钮就用它） |
| `conversation.input.dock` / `conversation.composer.dock` | list / session | composer 上方/下方的横条 |

另：`shell.overlay`（list/root，dsh-workspace-kit  Spotlight 用的整窗 overlay 位）存在于 layout 包。

**关键缺口：不存在 per-session-row 的 slot。** 会话行（`SessionNodeItem`）由 `dsh-client-ui-workspace` 内部渲染，行事实只有：pendingInteraction 状态点、标题、相对时间、行菜单、完成绿点、以及上面说的闹钟。第三方无法通过官方 slot 往行里插组件。

---

## 3. 社区生态现状（重点：有没有现成插件）

### 3.1 分发渠道（说明生态规模）

- **插件市场**：[dshmarket](https://github.com/dsh-market/dsh-market)（`dsh plugin --profile web add dshmarket`，装完在 设置 → 插件市场 里浏览/一键安装，自称收录 2300+ 插件）；
- **精选注册表**：[awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)（[awesome-dsh-plugin.com](https://awesome-dsh-plugin.com)，市场的数据源）；
- **让 agent 帮你找插件**：`dsh-find-plugin`；
- 安装命令统一为 `dsh plugin --profile web add <包名 | github:owner/repo | 本地路径 | .tgz>`（本地 `dsh-plugin-manager` README 核实：registry 名走 `pnpm view`，也支持 git/tarball/绝对路径）。

### 3.2 「侧边栏会话行定制」的四条既有技术路线（按官方程度排序）

1. **影子替换官方 slot（最官方）—— `ice5kysl/dsh-workspace-kit`**
   以 **priority -1** 注册进 `sidebar.workspaces`，渲染自己的整个浏览器（低优先级渲染、仅同优先级冲突，官方允许的「整体替换」路线），并提供一键切回官方。代价是官方浏览器能力（搜索、分组、对话框等）要自己实现（其 known-limitations 有列）。**这是唯一能「名正言顺」拿到每个会话行渲染权的路线**，天然支持行高亮 + 悬停 tooltip。
2. **bundle 转换 / 锚点注入 —— `enterhalf/dsh-session-colorful-unread-pin-jobs`**
   对 `dsh-client-ui-workspace` 的 `client.js` 做锚点字符串替换，给会话行加类、给标题上色（未读蓝/绿、置顶黄、后台任务紫渐变），host 侧 HTTP 路由 + `session/event` 订阅做状态，设置页列出全部未读会话。**与你的需求最同构**：它就是「host 记账 + 侧边栏行按状态染色 + 设置页清单」。已知限制：依赖 bundle 锚点，DSH 升级可能失效（有 `npm run check-anchors` 守卫，失配时安全降级）。
3. **DOM 挂载 —— `minivv/dsh-activity-bell`**
   作者原话：「官方没有在搜索按钮旁留扩展位，所以铃铛是挂到 DOM 上的」。铃铛+红色角标，点击把左侧列表换成「按天分组的活动列表」（未读行首绿点），复用官方行的盒子尺寸。证明纯客户端 DOM 增强路线可行且被社区接受。
4. **纯客户端零 host —— `Luaphes/dsh-web-attention-badge`**
   一个手写 client bundle 读官方 sessions store，做边框 pill、(N) 标签页标题、鲸鱼 favicon 变色（琥珀=等你输入，绿=完成未看）。说明「只读官方 store + 零配置」的最小插件形态。

同类的还有 `KannaKuron/dsh-better-workspace`（影子替换侧边栏 + 每行外观自定义：颜色/发光/字重/图标）、`FeatherHunter/dsh-im-companion`（给官方会话状态加蓝/橙/红导轨标记和左徽章）。

### 3.3 相邻需求（供参考，不是同一需求）

- **YOLO**（`dsh-plugin-yolo`，npm）：从对话里**自动抽取**待办/目标/里程碑/提醒规则（不要求固定关键词），按工作区持久化在 `.dsh/yolo/`，到期提醒进它自己的通知和专属会话（不插入你正在干的会话），左侧边栏底部按钮打开看板。**这是「自然语言 → 事项 → 提醒」最完整的现成品**，但它不做「侧边栏对应会话行高亮 + 悬停 tooltip」。
- **dsh-session-guard**（drscrewdriver）：会话级「暂停/继续」（高峰自动停会话省 token），其「暂停会话」按钮注册在 `conversation.input.right` slot，状态经 SSE + 轮询。证明 per-session 暂停按钮 + HTTP/SSE 通道的工程范式。
- **dph-taskboard**：侧边栏里把会话拖进 todo/doing/review/done 列。

### 3.4 在精选列表全文中检索的结果

对 awesome-dsh-plugin 全表（约 2300 条）按 `suspend|挂起|reminder|提醒|session.*(badge|highlight)|todo|待办|跟进` 等模式检索，**没有任何条目同时覆盖「自然语言登记 + 侧边栏行高亮 + 悬停提醒」**。最相关的就是本文引用的这几个。

---

## 4. 自研可行性：本地源码核实的架构事实

### 4.1 插件形态（官方文档 + 本地包核实）

- 插件 = 导出 `apply(ctx)` 的 cordis 模块；bundle 包在 `package.json` 声明 `dsh.bundle.patch`（指向 `cordis.patch.yml`），浏览器半边声明 `dsh.client`（`platform: 'web'` + `./client` export）。
- **一个 package 一个 loader entry，可同时带 host（node）和 browser（client）两张脸**（`dsh-workspace-kit` 的 `lib/index.js` + `lib/client.js` 模式，esbuild/tsdown 构建）。
- 安装：`dsh plugin --profile web add <spec>`；本地开发直接 `add <本地目录>`（pnpm link）。
- 开发热更新：client 插件改完后，在 DSH 源码检出里跑 `pnpm run dev:web` 可无刷新热替换（本地 `dsh-client-hmr` README）；独立插件包一般是 rebuild + 刷新页面（colorful-unread 插件甚至会写回 bundle 后调 `clientModules.rebuilt(id)`）。

### 4.2 「自然语言描述」的三种捕获机制（按推荐排序）

1. **注册模型工具**（官方 schedule、YOLO 的做法）：用户自然语言 → 模型理解 → 调 `suspend_session({ note, ... })` → 工具返回确认（可带结构化回显）。优点：零关键词、模型兜底解析；注意工具声明会进每次请求（token 成本小）。
2. **Slash 命令**（`ctx.commands.register`，session-guard 的 `/pause` `/resume`）：显式但可靠，可作为工具的补充入口。
3. **消息钩子**（监听 `session/event` 白名单事件，colorful-unread 的做法）：适合「自动推断」，但对「挂起」这种需要确认语义的动作容易误判，不建议单独用。

### 4.3 持久化 / 浏览器读模型三选一

| 方案 | 机制 | 优点 | 代价 |
|---|---|---|---|
| **Session Projection**（`@deepseek-ai/dsh-session-projection`，todo/goal/schedule 都用它） | host 注册投影单元，`wire.view` 产出客户端视图，快照+变更推送 | 官方读模型正道；冷读有 checkpoint 缓存；todo 就是这么做的 | 要学会 unit/stateSchema/stateVersion 契约；**投影值本身不会自动渲染到侧边栏行**（见 4.4） |
| **host HTTP 路由 + 浏览器 store**（colorful-unread 模式） | `ctx.webServer.register` 挂 `GET/POST /your-plugin/*`，客户端轮询/SSE 对账 | 完全自主； colorunread 已验证 | 自己管一致性和存储（它用 `~/.dsh/storages/<name>/*.json` 原子写） |
| **纯浏览器 localStorage**（workspace-kit 的软归档） | `defineStore` + persist | 零 host 代码 | 不跨机器/跨浏览器，重启 dsh 进程也在（localStorage 在浏览器侧），但换浏览器丢 |

建议：**投影或 host 路由二选一做权威存储**（要跨重启/服务端语义），UI 状态放浏览器 store。

### 4.4 关键限制（必须知道）

- **侧边栏会话行没有第三方 slot**；行内事实硬编码，`hasActiveSchedule` 只读官方 `schedule` 投影 key。想在行上做高亮+tooltip，只有：影子替换 `sidebar.workspaces`（官方路线，工作量大）、bundle 转换/DOM 注入（社区路线，随 DSH 升级可能失效）、或退而求其次用 `sidebar.toggle.badge` / `sidebar.footer.action` / `sidebar.panellist` 做「会话清单级」呈现（碰不到行本身）。
- 官方行的「重命名/分叉」等操作未开放给插件（activity-bell README 明确说官方没开放）。
- 会话归档是注册表级的（archiveSession 有 inject），可见性过滤要考虑。

### 4.5 与本机环境的兼容性

- 本机 dsh 0.1.6-alpha.2；YOLO 要求 ≥0.1.5-rc.1（满足），dshmarket 要求 ≥0.1.0-rc.6（满足）。
- `dsh-client-hmr` 的无刷新热替换只对「同一 DSH 源码检出 + `pnpm run dev:web`」有效；独立插件包开发走 rebuild+刷新。

---

## 5. 自研方案建议（MVP → 完整版）

**建议包名**：`dsh-session-suspend` / `dsh-parked-sessions`（npm 可注册即可）。

### MVP（1-2 天，全部走官方 slot，零 bundle  hack）

- **Host 脸**：注册模型工具 `suspend_session(note: string)` + `resume_session()` + `list_suspended()`；存储用 session projection（key 如 `suspend`，`wire.view` 出 `{ notes: [...] }`）或 `~/.dsh/storages/<pkg>/suspend.json` + 一个 `GET /<pkg>/list` 路由。
- **Browser 脸**：
  - `sidebar.toggle.badge`：有挂起会话时折叠栏显示计数；
  - `sidebar.footer.action`：「📌 挂起事项 (N)」按钮 → 打开一个面板（或注册 `sidebar.panellist` 全局面板页），列出所有被挂起的会话（标题+备注+时间），点击跳转；
  - `conversation.session.header.actions`：会话内「添加/查看挂起备注」按钮（capture 的主入口之一，配合自然语言工具互为备份）；
  - capture 主入口：对话里直接说「先挂起，等联调完了提醒我重启硬件」→ 模型调工具 → 工具结果回显确认。
- 这一版不碰会话行，升级安全性 100%。

### 完整版（补齐「行高亮 + 悬停 tooltip」）

在 MVP 之上二选一：

- **A. DOM 增强（推荐先做）**：客户端读挂起清单 → 对侧边栏行 DOM 加高亮 class（左侧色条/背景，参考 colorful-unread 的「只加类、不接管」增量哲学）+ 悬停 tooltip（备注文本/创建时间/「打开」「清除」动作）。做好「选择器失配即静默降级」。类名用官方 bundle 的哈希类（`..._sessionRow`）有升级风险，可用「行内 title 文本匹配 session 标题」或数据属性兜底。
- **B. 影子替换 `sidebar.workspaces`（priority -1）**：照抄 `dsh-workspace-kit` 的注册方式，自己渲染会话行——高亮、tooltip、行菜单全可控，且是官方允许的路径；代价是实现分组/搜索/对话框，并随官方浏览器演进。

### 分发

-  npm publish 预构建 `lib/`（最省事）；或 GitHub + `prepare` 构建脚本（用户需在 `pnpm-workspace.yaml` 批准 build scripts）；或 `pnpm pack` tarball / 本地路径自用。
-  想进生态：往 [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin) 提 PR，次日自动进入 dshmarket 市场。

---

## 6. 参考资料

**官方文档**
- [安排会话内提醒（schedule）](https://deepseek-harness.github.io/deepseek-harness/guide/schedule)
- [第一个 Harness 插件](https://deepseek-harness.github.io/deepseek-harness/develop/basic/) / [打包与安装插件](https://deepseek-harness.github.io/deepseek-harness/develop/basic/publish)
- 本地包文档（0.1.6-alpha.2）：`dsh-schedule`、`dsh-session-projection`、`dsh-client-ui-sidebar`、`dsh-client-ui-conversation`、`dsh-client-ui-workspace`、`dsh-plugin-manager`、`dsh-client-hmr` 的 README 与 `lib/types/**/*.d.ts`

**社区实现（按相关度）**
- [hanshanyike/dsh-yolo](https://github.com/hanshanyike/dsh-yolo)（npm: `dsh-plugin-yolo`）——自然语言事项+提醒+看板
- [ice5kysl/dsh-workspace-kit](https://github.com/ice5kysl/dsh-workspace-kit) —— `sidebar.workspaces` priority -1 影子替换（官方整替换路线）
- [enterhalf/dsh-session-colorful-unread-pin-jobs](https://github.com/enterhalf/dsh-session-colorful-unread-pin-jobs) —— 侧边栏行按状态染色（bundle 转换路线）
- [minivv/dsh-activity-bell](https://github.com/minivv/dsh-activity-bell) —— 侧边栏铃铛角标+活动列表（DOM 挂载路线）
- [Luaphes/dsh-web-attention-badge](https://github.com/Luaphes/dsh-web-attention-badge) —— 纯客户端注意力徽章（tab/favicon）
- [drscrewdriver/dsh-session-guard](https://github.com/drscrewdriver/dsh-session-guard) —— 会话级暂停/冻结 + `conversation.input.right` 按钮范式
- [KannaKuron/dsh-better-workspace](https://github.com/KannaKuron/dsh-better-workspace) —— 侧边栏影子替换+每行外观自定义
- 生态入口：[dsh-market/dsh-market](https://github.com/dsh-market/dsh-market)、[awesome-dsh-plugin/awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)、[awesome-dsh-plugin.com](https://awesome-dsh-plugin.com)
